---
layout: post
title: 1. MVP 数据库设计：用户 / 模型 / 会话
date: 2026-04-27
author: KeLes
related_records:
  - label: breakpoint-resume
    path: "_posts/Orbit 开发日志/note/2026-04-29-1. 流式传输的实现.md"
    reason: "补充说明断点续传如何依赖消息状态与恢复流程。"
  - label: long-session-summary
    path: "_posts/Orbit 开发日志/note/2026-04-27-2. 长会话摘要的实现.md"
    reason: "补充说明长会话摘要如何配合 conversations 与 messages 表。"
---

# 1. MVP 数据库设计：用户 / 模型 / 会话

Orbit 是一个面向大模型应用开发的 Agent 项目。本文作为开发日志的第一篇，主要梳理 MVP （最小可行性产品）阶段的核心架构与数据模型设计。第一步的核心目标是构建一个比较坚实的底层基座。跑通“用户 ↔ 模型配置 ↔ 多轮会话 ↔ LangGraph 引擎”的基本业务逻辑闭环。

## 1. 功能介绍
MVP 阶段主要提供以下三个基础能力：

### 1.1 用户注册与登陆
系统需要具备多用户能力。当前版本聚焦核心业务逻辑，仅采用本地邮箱注册和登录机制。通过分配全局唯一的用户 ID 来隔离不同租户间的私有数据资源。

### 1.2 模型配置与接入
避免将系统绑定在单一的大模型供应商上。用户可以自定义 LLM 接入配置，包括选择 Provider（OpenAI、Anthropic、Ollama 等）、指定具体模型名称、配置自定义 Base URL（以支持代理网关或本地模型），并注入个人的 API Key。支持多套配置并存与快速切换。

### 1.3 会话管理
提供类似 ChatGPT 的多轮对话管理能力。支持会话的创建与查询、消息状态（生成中、已完成、失败等）的追踪；并针对长文本会话引入基于 token 或消息数阈值的摘要压缩（Summarization）机制，防止上下文超出模型窗口限制。

## 2. 数据模型设计
为了支撑上述功能，我们在 PostgreSQL 中设计了四张核心业务表：`users`、`llm_configs`、`conversations` 和 `messages`。

核心 ER 关系为：**用户**拥有**多套模型配置**和**多个会话**，**会话包含多条消息记录**。

### 2.1 `users`

`users` 保存系统用户的基础身份信息。
它是 `llm_configs` 和 `conversations` 的归属主体，用来完成 MVP 数据模型闭环。

字段说明：

| 字段              | 说明                                                  |
| --------------- | --------------------------------------------------- |
| `id`            | 用户 ID                                               |
| `email`         | 用户邮箱，用作主要登录标识或展示标识                                  |
| `display_name`  | 用户展示名                                               |
| `avatar_url`    | 用户头像地址                                              |
| `metadata`      | 用户扩展信息                                              |
| `is_enabled`    | 是否启用，便于临时禁用账号                                       |
| `archived_at`   | 软删除时间                                               |
| `created_at`    | 创建时间                                                |
| `updated_at`    | 更新时间                                                |

#### 设计说明

`users` 是当前 MVP 的顶层业务实体。主要承担三个职责：

```text
1. 标识用户
2. 作为 llm_configs 的归属主体
3. 作为 conversations 的归属主体
```

### 2.2 `llm_configs`

`llm_configs` 保存用户的 LLM 接入配置。
它记录某个用户如何连接某个模型服务。

后端根据这张表构造 LangChain chat model，例如 OpenAI、Anthropic、Google GenAI、Ollama 或 OpenAI-compatible provider。

```sql
create table llm_configs (
  id uuid primary key default gen_random_uuid(),

  user_id uuid not null references users(id) on delete cascade,

  name varchar(100) not null,
  provider varchar(50) not null,
  model varchar(120) not null,

  base_url text,

  api_key_ciphertext text,

  provider_options jsonb not null default '{}'::jsonb,

  is_default boolean not null default false,
  is_enabled boolean not null default true,
  archived_at timestamptz,

  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),

  constraint ck_llm_configs_provider_nonempty
    check (length(trim(provider)) > 0),

  constraint ck_llm_configs_model_nonempty
    check (length(trim(model)) > 0)
);

create unique index uq_llm_configs_user_name_active
on llm_configs(user_id, name)
where archived_at is null;

create unique index uq_llm_configs_user_default_active
on llm_configs(user_id)
where is_default = true and archived_at is null;

create index idx_llm_configs_user_enabled
on llm_configs(user_id, is_enabled)
where archived_at is null;
```

字段说明：

| 字段                   | 说明                                                                        |
| -------------------- | ------------------------------------------------------------------------- |
| `id`                 | 模型配置 ID                                                                   |
| `user_id`            | 配置所属用户                                                                    |
| `name`               | 用户自定义展示名，例如“OpenAI 主账号”“本地 Ollama”                                        |
| `provider`           | 模型供应商，例如 `openai`、`openai_compatible`、`anthropic`、`google_genai`、`ollama` |
| `model`              | 实际传给 LangChain / provider SDK 的模型名                                        |
| `base_url`           | 自定义 API 地址，支持代理网关、OpenAI-compatible 服务和本地模型                               |
| `api_key_ciphertext` | 加密后的 API Key，不保存明文                                                        |
| `provider_options`   | Provider 特有参数，例如 `temperature`、`timeout`、`api_version`、`deployment_name`  |
| `is_default`         | 当前用户默认模型配置                                                                |
| `is_enabled`         | 是否启用，便于临时禁用失效配置                                                           |
| `archived_at`        | 软删除时间                                                                     |
| `created_at`         | 创建时间                                                                      |
| `updated_at`         | 更新时间                                                                      |

#### 设计说明

`llm_configs` 的定位是**用户模型接入配置**。

会话中途如果用户切换模型，只应该影响后续调用，不应该改变历史消息的模型归属。因此，历史消息仍然需要在 `messages` 表里保存当次调用的 `provider` 和 `model` 快照。

`provider_options` 第一阶段可以保持一个 JSONB 字段，避免过早拆分。后续如果配置复杂，可以拆成：

```text
connection_options
generation_defaults
```

例如：

```json
{
  "connection": {
    "timeout": 60,
    "api_version": "2024-xx-xx",
    "deployment_name": "xxx"
  },
  "generation": {
    "temperature": 0.7,
    "max_tokens": 2048
  }
}
```

### 2.3 `conversations`

`conversations` 保存会话元信息。
它记录会话标题、所属用户、当前默认模型配置、聊天模式、摘要以及 LangGraph 使用的 `thread_id`。

> LangGraph checkpointer 使用 `thread_id` 作为保存和恢复 checkpoint 的关键标识。checkpointer 会基于 `thread_id` 存取 checkpoint。

字段说明：

| 字段                      | 说明                                            |
| ----------------------- | --------------------------------------------- |
| `id`                    | 业务会话 ID                                       |
| `user_id`               | 会话所属用户                                        |
| `thread_id`             | LangGraph checkpointer 使用的 thread ID          |
| `title`                 | 会话标题                                          |
| `llm_config_id`         | 当前会话默认使用的模型配置                                 |
| `chat_mode`             | 聊天模式，MVP 使用 `chat`，后续可扩展 `rag`、`agent`、`tool` |
| `summary`               | 长会话压缩摘要                                       |
| `summary_updated_at`    | 最近一次摘要更新时间                                    |
| `summary_message_count` | 当前摘要覆盖到的消息数量                                  |
| `metadata`              | 会话扩展信息                                        |
| `archived_at`           | 软删除时间                                         |
| `created_at`            | 创建时间                                          |
| `updated_at`            | 更新时间                                          |

#### 设计说明

`conversations.id` 是会话的业务会话 ID。
`conversations.thread_id` 是 LangGraph runtime 使用的 thread 标识。


```text
conversation.id = 产品侧会话 ID
thread_id       = LangGraph checkpointer 使用的运行时 ID
```

这样后续如果一个会话需要多个 graph thread，也不会被当前设计锁死。例如：

```text
chat thread
summary thread
background task thread
research thread
```

`summary` ：业务侧长会话压缩结果，保存的是产品侧可控、可查询、可迁移的上下文压缩结果。

LangGraph checkpoint 保存运行时 state。

### 2.4 `messages`

`messages` 保存会话内的消息。
它是 UI 展示、历史查询、导出、审计和成本统计的事实源。

字段说明：

| 字段                     | 说明                                                             |
| ---------------------- | -------------------------------------------------------------- |
| `id`                   | 消息 ID                                                          |
| `conversation_id`      | 所属会话                                                           |
| `sequence_no`          | 会话内消息顺序号                                                       |
| `langgraph_message_id` | LangChain / LangGraph message ID，可选保存，用于后续和 graph state 对齐     |
| `role`                 | 消息角色，使用 `system`、`user`、`assistant`、`tool`                   |
| `content`              | 消息正文，MVP 阶段主要使用纯文本                                             |
| `content_parts`        | 预留的结构化内容块，后续支持多模态、附件、工具结果、结构化输出                                |
| `status`               | 消息状态，例如 `streaming`、`completed`、`cancelled`、`failed`、`partial` |
| `llm_config_id`        | 生成该消息时使用的模型配置                                                  |
| `provider`             | 生成该消息时使用的 Provider 快照                                          |
| `model`                | 生成该消息时使用的模型名快照                                                 |
| `token_usage`          | Token 用量统计                                                     |
| `response_metadata`    | LangChain 或底层 SDK 返回的结构化元信息                                    |
| `created_at`           | 创建时间                                                           |

#### 设计说明

`sequence_no` 用来保证会话内消息顺序稳定。
避免只依赖 `created_at` 排序，因为流式生成、重试、并发写入时，时间戳排序可能不够明确。

推荐读取消息时使用：

```sql
select *
from messages
where conversation_id = :conversation_id
order by sequence_no asc;
```

`langgraph_message_id` 是可选字段。
MVP 阶段可以不强依赖它，但先保留字段有价值。后续如果你需要删除、压缩、映射 LangGraph state 内部消息，就可以用它做关联。

`content_parts` 是扩展字段。
MVP 阶段所有消息都写入 `content`，`content_parts` 保持空数组。后续如果支持图片、文件、tool call、artifact、structured output，再逐步使用。

## 3. 实现思路

当前版本规划的基础业务链路为：
```text
用户注册 / 登录
  -> 创建模型配置
  -> 创建会话
  -> 发送消息
  -> 调用模型生成回复
  -> 保存消息记录
  -> 查询历史会话
```

### 3.1 用户注册与登陆
MVP 阶段采用邮箱注册与登陆机制。用户注册时，系统创建一条 `users` 记录，并生成全局唯一的 `user_id`。后续所有模型配置、会话和消息数据都通过该 `user_id` 进行隔离。

基础流程如下：

```text
用户提交邮箱和密码
  -> 校验邮箱格式和唯一性
  -> 对密码进行哈希处理
  -> 写入 users 表
  -> 返回用户基础信息或登录凭证
```

登陆流程如下：

```text
用户提交邮箱和密码
  -> 根据邮箱查询用户
  -> 校验账号状态
  -> 校验密码哈希
  -> 创建登录态
  -> 返回用户信息
```

### 3.2 模型配置管理
系统在创建会话或发起模型调用前，需要确认当前用户至少有一套可用的 `llm_configs` 记录。

`llm_configs` 记录用户如何连接某个模型服务，包括 Provider、模型名、Base URL、API Key 密文以及 Provider 特有参数。

基础流程如下：
```text
用户创建模型配置
  -> 校验 provider 和 model
  -> 加密 API Key
  -> 写入 llm_configs 表
  -> 设置默认配置
```

一次有效的模型配置至少需要包含：
```text
provider
model
api_key_ciphertext
```

对于需要自定义接入地址的场景，可以通过 `base_url` 指定 API 地址，例如代理网关、本地模型服务、OpenAI-compatible 服务，或其他支持自定义 Endpoint 的 Provider。

`provider_options` 用于保存不同 Provider 的差异化参数，例如：
```json
{
  "temperature": 0.7,
  "max_tokens": 2048,
  "timeout": 60
}
```

后端在发起模型调用前，根据 llm_configs 构造对应的模型客户端：
```text
读取 llm_config
  -> 解密 API Key
  -> 根据 provider 选择适配器
  -> 注入 model、base_url、provider_options
  -> 创建 chat model 实例
```

`is_default` 表示当前用户的默认模型配置。创建新会话时，如果用户没有显式选择模型配置，系统使用默认配置作为会话的初始模型。

用户可以停用或软删除模型配置。停用后的配置不再允许用于新的模型调用，但历史消息仍保留当时的 `provider` 和 `model` 快照，保证历史记录可追溯。

API Key 加解密过程：
```text
保存 API Key：
用户输入 API Key
  -> 后端校验格式
  -> 使用服务端 encryption key 加密
  -> 保存到 llm_configs.api_key_ciphertext

使用 API Key：
读取 llm_configs
  -> 解密 api_key_ciphertext
  -> 构造 provider SDK client
  -> 调用模型
  -> API Key 不返回前端
```

### 3.3 会话创建与管理
`conversations` 保存会话级别的元信息，包括标题、所属用户、默认模型配置、聊天模式和摘要信息。

创建会话时，系统需要确定三个核心信息：
```text
user_id
llm_config_id
title
```
基础流程如下：
```text
用户点击新建会话
  -> 读取用户默认模型配置
  -> 创建 conversations 记录
  -> 返回 conversation_id
```

`conversations.llm_config_id` 表示当前会话默认使用的模型配置。用户在会话中切换模型时，只影响后续消息生成，不修改历史消息的模型快照。

会话列表查询主要依赖：
```sql
select *
from conversations
where user_id = :user_id
  and archived_at is null
order by updated_at desc;
```

`updated_at` 用于控制会话列表排序。每次新增消息、修改标题或更新摘要时，都应该同步更新会话的 `updated_at`。

`chat_mode` 预留不同对话形态。MVP 阶段默认使用 `chat`，后续可以扩展为 `rag`、`agent` 或 `tool`。

### 3.4 多轮消息写入
`message` 是会话内容的事实源。前端显示历史消息、导出聊天记录、统计 Token 用量、排查模型调用问题，都应该以 `message` 为准。

一次完整的用户提问流程如下：

```text
用户发送消息
  -> 生成下一条 sequence_no
  -> 写入 user 消息
  -> 创建 assistant 占位消息
  -> 调用模型生成回复
  -> 更新 assistant 消息内容和状态
  -> 更新 conversations.updated_at
```

`sequence_no` 用于保证会话内消息顺序稳定。读取消息时统一按照 `sequence_no` 排序：

```text
select *
from messages
where conversation_id = :conversation_id
order by sequence_no asc;
```

用户消息写入后，状态直接设为 `completed`。

Assistant 消息可以先写入一条占位记录：
```text
role = assistant
content = ''
status = streaming
```

模型生成结束后，再将完整内容写回该记录，并将状态更新为 `completed`。

如果调用失败，则更新为：

```text
status = failed
```

如果生成过程中断，但已有部分内容可用，则更新为：

```text
status = partial
```

这样前端可以根据 `status` 展示不同状态：

| 状态          | 前端表现             |
| ----------- | ---------------- |
| `streaming` | 正在生成             |
| `completed` | 正常展示             |
| `failed`    | 展示失败提示，允许重试      |
| `partial`   | 展示部分内容，提示生成未完整结束 |
| `cancelled` | 展示用户主动取消生成       |

> [!NOTE]
> 关于 `partial`:
> 
> 表示的是模型生成过程没有完整结束，但已经产生了可展示的部分内容。存在的意义是避免把已经生成的内容直接丢掉。
> 
> 比如：
>
> ```text
> 网络中断
> 客户端断开 （注：关于这个场景，后续版本应该考虑断点重连的实现）
> 上游模型中途断流
> 服务端超时
> 用户刷新页面
> ```

### 3.5 模型调用与消息生成
生成 Assistant 回复时，后端从会话读取当前使用的 `llm_config_id`，并获取对应的模型配置。

调用流程如下：
```text
读取 conversation
  -> 确定 llm_config_id
  -> 读取 llm_configs
  -> 组装历史消息上下文
  -> 调用模型服务
  -> 保存 assistant 回复
```
在保存 Assistant 消息时，需要同时写入：
```text
llm_config_id
provider
model
token_usage
response_metadata
```

### 3.6 长会话摘要
当会话消息持续增加时，直接拼接全部历史消息会带来上下文长度和调用成本问题。

MVP 阶段可以先使用基于消息数量的摘要策略。系统通过 `conversations.summary_message_count` 记录当前摘要已经覆盖到哪一条消息。

基础策略如下：
```text
当会话消息数超过阈值
  -> 读取前 N 条未摘要消息
  -> 调用模型生成摘要
  -> 写入 conversations.summary
  -> 更新 summary_message_count
  -> 更新 summary_updated_at
```

下一次对话时，上下文组装逻辑为：
```text
System Prompt
  + summary
  + summary_message_count 之后的新消息
```
`summary` 是业务侧可控的长会话压缩结果，便于查询、迁移和调试。

第一阶段可以只使用消息数量阈值，例如 40 条。后续可以升级为混合策略：
```text
消息数量阈值
Token 数量阈值
模型上下文窗口大小
最近消息保留数量
```
摘要功能不影响 `messages` 表中的原始消息。原始消息仍然完整保存，摘要只用于后续模型上下文组装。

关于长会话摘要的主流实现与后续实现计划，可阅读：[^long-session-summary]

## 4. 关键代码
### 4.1 建表语句

```sql
create table users (
  id uuid primary key default gen_random_uuid(),

  email varchar(255) not null,
  password_hash text not null,

  display_name varchar(100),

  avatar_url text,

  metadata jsonb not null default '{}'::jsonb,

  is_enabled boolean not null default true,
  archived_at timestamptz,

  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),

  constraint ck_users_email_nonempty
    check (length(trim(email)) > 0)
);

create unique index uq_users_email_active
on users(lower(email))
where archived_at is null;

create index idx_users_enabled
on users(is_enabled)
where archived_at is null;
```

```sql
create table llm_configs (
  id uuid primary key default gen_random_uuid(),

  user_id uuid not null references users(id) on delete cascade,

  name varchar(100) not null,
  provider varchar(50) not null,
  model varchar(120) not null,

  base_url text,

  api_key_ciphertext text,

  provider_options jsonb not null default '{}'::jsonb,

  is_default boolean not null default false,
  is_enabled boolean not null default true,
  archived_at timestamptz,

  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),

  constraint ck_llm_configs_provider_nonempty
    check (length(trim(provider)) > 0),

  constraint ck_llm_configs_model_nonempty
    check (length(trim(model)) > 0)
);

create unique index uq_llm_configs_user_name_active
on llm_configs(user_id, name)
where archived_at is null;

create unique index uq_llm_configs_user_default_active
on llm_configs(user_id)
where is_default = true and archived_at is null;

create index idx_llm_configs_user_enabled
on llm_configs(user_id, is_enabled)
where archived_at is null;
```

```sql
create table conversations (
  id uuid primary key default gen_random_uuid(),

  user_id uuid not null references users(id) on delete cascade,

  thread_id text not null default gen_random_uuid()::text,

  title varchar(200),

  llm_config_id uuid references llm_configs(id) on delete set null,

  chat_mode varchar(32) not null default 'chat',

  summary text,
  summary_updated_at timestamptz,
  summary_message_count integer not null default 0,

  metadata jsonb not null default '{}'::jsonb,

  archived_at timestamptz,

  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),

  constraint ck_conversations_chat_mode
    check (chat_mode in ('chat', 'rag', 'agent', 'tool'))
);

create unique index uq_conversations_thread_id
on conversations(thread_id);

create index idx_conversations_user_updated
on conversations(user_id, updated_at desc)
where archived_at is null;
```

```sql
create table messages (
  id uuid primary key default gen_random_uuid(),

  conversation_id uuid not null
    references conversations(id) on delete cascade,

  sequence_no integer not null,

  langgraph_message_id text,

  role varchar(32) not null,

  content text not null default '',

  content_parts jsonb not null default '[]'::jsonb,

  status varchar(32) not null default 'completed',

  llm_config_id uuid references llm_configs(id) on delete set null,

  provider varchar(50),
  model varchar(120),

  token_usage jsonb not null default '{}'::jsonb,
  response_metadata jsonb not null default '{}'::jsonb,

  created_at timestamptz not null default now(),

  constraint ck_messages_role
    check (role in ('system', 'user', 'assistant', 'tool')),

  constraint ck_messages_status
    check (status in ('streaming', 'completed', 'cancelled', 'failed', 'partial'))
);

create unique index uq_messages_conversation_sequence
on messages(conversation_id, sequence_no);

create index idx_messages_conversation_created
on messages(conversation_id, created_at);

create index idx_messages_conversation_sequence
on messages(conversation_id, sequence_no);

create index idx_messages_langgraph_message_id
on messages(langgraph_message_id)
where langgraph_message_id is not null;
```

### 4.2 后台初始化、建表迁移与 DAO

这一节记录数据层相关代码，包括：应用初始化、配置加载、数据库 Session、Alembic 建表、 ORM 模型和 Repository/DAO。

当前数据层相关代码位置：

```text
backend/app/main.py
backend/app/core/config.py
backend/app/db/base.py
backend/app/db/session.py
backend/alembic/env.py
backend/alembic/versions/20260427_0001_create_mvp_tables.py
backend/app/models
backend/app/repositories
```

#### 4.2.1 后台应用初始化

后端使用应用工厂创建 FastAPI 实例。所有 v1 API 统一挂载到 `settings.api_prefix`，当前默认为 `/api/v1`。

```python
# backend/app/main.py
def create_app() -> FastAPI:
    # 应用工厂：后续测试或脚本可以复用这里创建独立 FastAPI 实例。
    app = FastAPI(title=settings.app_name)

    # 前后端分离开发时，前端 dev server 需要跨域访问后端 API。
    app.add_middleware(
        CORSMiddleware,
        allow_origins=settings.cors_origins,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    # 所有业务 API 统一挂到 /api/v1，便于后续版本演进。
    app.include_router(api_router, prefix=settings.api_prefix)
    return app


app = create_app()
```

配置通过 `pydantic-settings` 从环境变量和 `.env` 中读取，并统一使用 `ORBIT_` 前缀。

```python
# backend/app/core/config.py
class Settings(BaseSettings):
    # 统一使用 ORBIT_ 前缀读取环境变量，避免和系统变量或其他项目变量冲突。
    model_config = SettingsConfigDict(env_prefix="ORBIT_", env_file=".env", extra="ignore")

    app_name: str = "Orbit Backend"
    env: str = "local"
    api_prefix: str = "/api/v1"
    database_url: str = "postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/orbit"
    auth_secret_key: str = "orbit-local-dev-secret-change-me-32-bytes-min"
    encryption_secret_key: str = "orbit-local-encryption-secret"
    access_token_expire_minutes: int = 60 * 24 * 7
    cors_origins: list[str] = Field(
        default_factory=lambda: ["http://localhost:5173", "http://127.0.0.1:5173"]
    )
```

#### 4.2.2 数据库 Session 初始化

ORM 基类保持极简，所有模型都继承同一个 `Base`，便于 Alembic 收集 metadata。

```python
# backend/app/db/base.py
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    pass
```

数据库连接使用 `SQLAlchemy async engine` 和 `async_sessionmaker`。

```python
# backend/app/db/session.py
engine = create_async_engine(settings.database_url, pool_pre_ping=True)
AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)

async def get_db_session() -> AsyncGenerator[AsyncSession, None]:
    # FastAPI 依赖函数：每个请求拿到一个独立的异步数据库会话。
    async with AsyncSessionLocal() as session:
        yield session
```

这里的 `get_db_session` 是 API 层和 Service 层获取数据库会话的统一入口。事务提交通常由 Service 层完成，Repository/DAO 只负责读写与 `flush`。

#### 4.2.3 Alembic 迁移初始化

Alembic 迁移配置复用应用的 `settings.database_url`，避免运行时连接地址和迁移连接地址不一致。

```python
# backend/alembic/env.py
config = context.config
# Alembic 使用同一份应用配置读取数据库地址，避免迁移配置和运行时配置漂移。
config.set_main_option("sqlalchemy.url", settings.database_url)

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata
```

为了让 Alembic 能发现全部 ORM 模型，`env.py` 会导入 `app.models`：

```python
# backend/alembic/env.py
from app.db.base import Base
from app import models  # noqa: F401
```

在线迁移使用异步引擎：

```python
# backend/alembic/env.py
async def run_migrations_online() -> None:
    # 项目运行时使用 asyncpg，因此 Alembic 在线迁移也走异步引擎。
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )

    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)

    await connectable.dispose()
```

当前第一条迁移为：

```text
backend/alembic/versions/20260427_0001_create_mvp_tables.py
```

它负责创建：

```text
pgcrypto extension
users
llm_configs
conversations
messages
相关唯一索引和查询索引
```

#### 4.2.4 ORM 模型映射

`users` 模型承担用户数据归属根节点的角色，同时在当前实现中保存 `password_hash`。

```python
# backend/app/models/user.py
class User(Base):
    # 用户是 MVP 的顶层租户实体，模型配置和会话都通过 user_id 隔离。
    __tablename__ = "users"

    id: Mapped[UUID] = mapped_column(primary_key=True, server_default=text("gen_random_uuid()"))
    email: Mapped[str] = mapped_column(String(255), nullable=False)
    password_hash: Mapped[str] = mapped_column(Text, nullable=False)
    display_name: Mapped[str | None] = mapped_column(String(100))
    avatar_url: Mapped[str | None] = mapped_column(Text)
    metadata_: Mapped[dict] = mapped_column("metadata", JSONB, nullable=False, server_default=text("'{}'::jsonb"))
    is_enabled: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=expression.true())
    archived_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now()
    )

    llm_configs = relationship("LLMConfig", back_populates="user")
    conversations = relationship("Conversation", back_populates="user")
```

`llm_configs` 模型保存用户级模型接入配置。

```python
# backend/app/models/llm_config.py
class LLMConfig(Base):
    # 用户的模型接入配置：provider / model / base_url / API Key 密文都在这里。
    __tablename__ = "llm_configs"

    id: Mapped[UUID] = mapped_column(primary_key=True, server_default=text("gen_random_uuid()"))
    user_id: Mapped[UUID] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    provider: Mapped[str] = mapped_column(String(50), nullable=False)
    model: Mapped[str] = mapped_column(String(120), nullable=False)
    base_url: Mapped[str | None] = mapped_column(Text)
    api_key_ciphertext: Mapped[str | None] = mapped_column(Text)
    provider_options: Mapped[dict] = mapped_column(
        JSONB, nullable=False, server_default=text("'{}'::jsonb")
    )
    is_default: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=expression.false())
    is_enabled: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=expression.true())
    archived_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
```

`conversations` 保存会话元信息，`messages` 保存聊天事实源。

```python
# backend/app/models/conversation.py
class Conversation(Base):
    # 会话元信息表：保存标题、默认模型配置、摘要和 LangGraph thread_id。
    __tablename__ = "conversations"

    id: Mapped[UUID] = mapped_column(primary_key=True, server_default=text("gen_random_uuid()"))
    user_id: Mapped[UUID] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    thread_id: Mapped[str] = mapped_column(Text, nullable=False, server_default=text("gen_random_uuid()::text"))
    title: Mapped[str | None] = mapped_column(String(200))
    llm_config_id: Mapped[UUID | None] = mapped_column(ForeignKey("llm_configs.id", ondelete="SET NULL"))
    chat_mode: Mapped[str] = mapped_column(String(32), nullable=False, server_default=text("'chat'"))
    summary: Mapped[str | None] = mapped_column(Text)
    summary_updated_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    summary_message_count: Mapped[int] = mapped_column(Integer, nullable=False, server_default=text("0"))
    metadata_: Mapped[dict] = mapped_column("metadata", JSONB, nullable=False, server_default=text("'{}'::jsonb"))
```

```python
# backend/app/models/message.py
class Message(Base):
    # 消息表是聊天历史的事实源，UI 展示、导出、审计都以这里为准。
    __tablename__ = "messages"
    __table_args__ = (
        UniqueConstraint("conversation_id", "sequence_no", name="uq_messages_conversation_sequence"),
    )

    id: Mapped[UUID] = mapped_column(primary_key=True, server_default=text("gen_random_uuid()"))
    conversation_id: Mapped[UUID] = mapped_column(
        ForeignKey("conversations.id", ondelete="CASCADE"), nullable=False
    )
    sequence_no: Mapped[int] = mapped_column(Integer, nullable=False)
    langgraph_message_id: Mapped[str | None] = mapped_column(Text)
    role: Mapped[str] = mapped_column(String(32), nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False, server_default=text("''"))
    content_parts: Mapped[list] = mapped_column(JSONB, nullable=False, server_default=text("'[]'::jsonb"))
    status: Mapped[str] = mapped_column(String(32), nullable=False, server_default=text("'completed'"))
    llm_config_id: Mapped[UUID | None] = mapped_column(ForeignKey("llm_configs.id", ondelete="SET NULL"))
    provider: Mapped[str | None] = mapped_column(String(50))
    model: Mapped[str | None] = mapped_column(String(120))
    token_usage: Mapped[dict] = mapped_column(JSONB, nullable=False, server_default=text("'{}'::jsonb"))
    response_metadata: Mapped[dict] = mapped_column(JSONB, nullable=False, server_default=text("'{}'::jsonb"))
```
#### 4.2.5 Repository / DAO

Repository 只处理数据访问，不处理业务编排。

`UserRepository` 只负责 `users` 表读写，密码哈希、登录态签发等逻辑放在 Service 层。

```python
# backend/app/repositories/user.py
class UserRepository:
    # Repository 只封装 users 表查询和写入，不处理密码或登录业务。
    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def get_by_id(self, user_id: UUID) -> User | None:
        # 只返回未归档用户，软删除账号不再参与鉴权和业务查询。
        statement = select(User).where(User.id == user_id, User.archived_at.is_(None))
        result = await self.session.execute(statement)
        return result.scalar_one_or_none()

    async def get_by_email(self, email: str) -> User | None:
        # 邮箱登录大小写不敏感，和数据库的 lower(email) 唯一索引保持一致。
        statement = select(User).where(
            func.lower(User.email) == email.lower(),
            User.archived_at.is_(None),
        )
        result = await self.session.execute(statement)
        return result.scalar_one_or_none()
```

`LLMConfigRepository` 封装模型配置的列表、计数、单条读取、默认配置切换和软删除。

```python
# backend/app/repositories/llm_config.py
class LLMConfigRepository:
    # Repository 保持薄层，只负责 llm_configs 表的数据库操作。
    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def list_active(self, user_id: UUID) -> list[LLMConfig]:
        # 默认配置排在最前面，方便前端直接展示当前默认模型。
        statement = (
            select(LLMConfig)
            .where(LLMConfig.user_id == user_id, LLMConfig.archived_at.is_(None))
            .order_by(LLMConfig.is_default.desc(), LLMConfig.created_at.desc())
        )
        result = await self.session.execute(statement)
        return list(result.scalars().all())

    async def unset_defaults(self, user_id: UUID) -> None:
        # 部分唯一索引要求同一用户只能有一个活跃默认配置，因此先清空旧默认。
        await self.session.execute(
            update(LLMConfig)
            .where(
                LLMConfig.user_id == user_id,
                LLMConfig.archived_at.is_(None),
                LLMConfig.is_default.is_(True),
            )
            .values(is_default=False, updated_at=func.now())
        )
```

`ConversationRepository` 负责会话元信息读写，不直接处理模型调用。

```python
# backend/app/repositories/conversation.py
class ConversationRepository:
    # ConversationRepository 只处理会话元信息，不直接处理模型调用。
    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def list_active(self, user_id: UUID) -> list[Conversation]:
        # 会话列表按最近更新时间倒序，符合聊天产品的常见展示方式。
        statement = (
            select(Conversation)
            .where(Conversation.user_id == user_id, Conversation.archived_at.is_(None))
            .order_by(Conversation.updated_at.desc())
        )
        result = await self.session.execute(statement)
        return list(result.scalars().all())

    async def touch(self, conversation_id: UUID) -> None:
        # 新增消息或更新摘要后刷新 updated_at，用于会话列表排序。
        await self.session.execute(
            update(Conversation)
            .where(Conversation.id == conversation_id)
            .values(updated_at=func.now())
        )
```

`MessageRepository` 负责消息事实源的读写，并保证会话内消息按照 `sequence_no` 稳定排序。

```python
# backend/app/repositories/conversation.py
class MessageRepository:
    # MessageRepository 负责消息事实源的读写，顺序由 sequence_no 保证。
    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def list_by_conversation(self, conversation_id: UUID) -> list[Message]:
        # 读取历史消息必须按 sequence_no 排序，避免时间戳并发写入导致顺序漂移。
        statement = (
            select(Message)
            .where(Message.conversation_id == conversation_id)
            .order_by(Message.sequence_no.asc())
        )
        result = await self.session.execute(statement)
        return list(result.scalars().all())

    async def next_sequence_no(self, conversation_id: UUID) -> int:
        # MVP 先用 max(sequence_no)+1；高并发发送时可升级为行锁或独立计数器。
        statement = select(func.coalesce(func.max(Message.sequence_no), 0) + 1).where(
            Message.conversation_id == conversation_id
        )
        result = await self.session.execute(statement)
        return int(result.scalar_one())
```
